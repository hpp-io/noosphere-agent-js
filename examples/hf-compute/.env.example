# Copy to .env on the compute host. HF_BIND_IP must be the host's Tailscale IP
# (100.x.y.z). Never 0.0.0.0 — the selling agent enforces x402 payment; anything
# that can reach the raw port gets the service for free.
HF_BIND_IP=100.x.y.z
